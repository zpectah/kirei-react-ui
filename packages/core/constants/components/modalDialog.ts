import { breakpointKeys } from 'types';

export const MODAL_DIALOG_ROOT = 'ModalDialog';
export const MODAL_DIALOG_BACKDROP = `${MODAL_DIALOG_ROOT}-backdrop`;

export const MODAL_DIALOG_DEFAULT_VALUES = {
  maxWidth: breakpointKeys.xl,
};
