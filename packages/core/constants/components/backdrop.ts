import { backdropBackgroundColorKeys } from 'types';

export const BACKDROP_ROOT = 'Backdrop';

export const BACKDROP_DEFAULT_VALUES = {
  as: 'div',
  background: backdropBackgroundColorKeys.dark,
};
