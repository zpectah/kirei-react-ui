import { breakpointKeys, spacingKeys } from 'types';

export const CONTAINER_ROOT = 'Container';

export const CONTAINER_DEFAULT_VALUES = {
  as: 'div',
  maxWidth: breakpointKeys.xl,
  xOffset: spacingKeys['2'],
};
