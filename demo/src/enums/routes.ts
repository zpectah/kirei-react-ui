export enum getStartedRouteKeys {
  overview = 'overview',
  installation = 'installation',
  usage = 'usage',
}

export enum customizationRouteKeys {
  theme = 'theme',
  palette = 'palette',
  typography = 'typography',
  breakpoints = 'breakpoints',
  components = 'components',
}

export enum apiRouteKeys {
  button = 'button',
  checkbox = 'checkbox',
  container = 'container',
  'control-label' = 'control-label',
  'icon-button' = 'icon-button',
  icons = 'icons',
  link = 'link',
  modal = 'modal',
  'modal-body' = 'modal-body',
  'modal-footer' = 'modal-footer',
  'modal-header' = 'modal-header',
  paper = 'paper',
  radio = 'radio',
  stack = 'stack',
  switch = 'switch',
  typography = 'typography',
}
