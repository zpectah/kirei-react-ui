import { flexJustifyContentKeys } from 'types';
import { MODAL_ROOT } from './modal';

export const MODAL_FOOTER_ROOT = `${MODAL_ROOT}Footer`;

export const MODAL_FOOTER_DEFAULT_VALUES = {
  justifyContent: flexJustifyContentKeys['space-between'],
};
