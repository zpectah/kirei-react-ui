export interface ThemeShape {
  borderRadius: {
    small: string;
    medium: string;
    large: string;
  };
  borderWidth: {
    button: string;
    input: string;
    divider: string;
    outline: string;
  };
}
