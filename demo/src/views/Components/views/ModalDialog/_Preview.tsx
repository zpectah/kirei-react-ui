import React from 'react';
// import { useUiContext } from 'styles';
import { Article, Section } from '../../../../components';

const PreviewView = () => {
  // const { theme } = useUiContext();

  return (
    <Article>
      <Section title="Modal Dialog default">Modal Dialog</Section>
    </Article>
  );
};

export default PreviewView;
