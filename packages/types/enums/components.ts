export enum kireiComponentsRootListKeys {
  button = 'button',
  container = 'container',
  link = 'link',
  stack = 'stack',
  typography = 'typography',
  modal = 'modal',
}

export enum kireiComponentsListKeys {
  button = 'button',
  container = 'container',
  link = 'link',
  stack = 'stack',
  typography = 'typography',
  modal = 'modal',
  modalBody = 'modalBody',
  modalFooter = 'modalFooter',
  modalHeader = 'modalHeader',
}
