import { paperBackgroundColorKeys, paperVariantKeys } from 'types';

export const PAPER_ROOT = 'Paper';

export const PAPER_DEFAULT_VALUES = {
  as: 'div',
  color: paperBackgroundColorKeys.paper,
  variant: paperVariantKeys.contained,
};
